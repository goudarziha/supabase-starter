import React, { useState, useEffect } from 'react';
import { FormControl, Box, Flex, Button } from '@chakra-ui/react';

const Contact = () => {
  const [isLoading, setIsLoading] = useState<boolean>(false);

  return <Box>hello</Box>;
};
export default Contact;
